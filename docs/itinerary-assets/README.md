# Itinerary assets

Event posters and related images for itinerary widgets (~600px wide preferred).

Served on GitHub Pages as:

`https://jhansenwmi.github.io/rumble-WMI-videos/itinerary-assets/…`

Referenced from `international-itinerary.json` (and later US) via `flyer` paths/URLs.

MediaSite will write resized posters here when that flow is built. Seed scrape still points many flyers at live CMS `Userfiles/Flyer-jpegs/`.
